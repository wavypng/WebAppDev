<%= form_with(model: prefix) do |form| %>
  <% if prefix.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prefix.errors.count, "error") %> prohibited this prefix from being saved:</h2>

      <ul>
        <% prefix.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :prefix, style: "display: block" %>
    <%= form.text_field :prefix %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
